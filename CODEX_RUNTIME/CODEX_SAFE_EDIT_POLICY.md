# Codex Safe Edit Policy

Codex must:
- avoid broad rewrites unless requested
- preserve public APIs unless change request allows changes
- run simple syntax/build checks by default
- not commit secrets
- not create production code in AppForge
- update or request registry updates after implementation

For UI work, Codex must implement approved Figma/specs and not invent a new aesthetic direction.
