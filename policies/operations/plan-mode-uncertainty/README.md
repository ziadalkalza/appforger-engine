# Plan Mode and Uncertainty

This folder defines how AppForge behaves when work is ambiguous, risky, expensive, destructive, unsupported, or likely to waste tokens.

Core rule:

```text
When uncertain, ask. When the task is large or risky, enter plan mode before real work. Do not speculate and do not perform irreversible work without approval.
```
