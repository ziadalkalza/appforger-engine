# GitHub Actions Policy

GitHub Actions may be used for validation, tests, context reindexing, release readiness checks, and report generation.

AppForge provides templates in the engine. Active workflows are generated into the relevant repo only when enabled by the user.

GitHub Actions must not expose secrets in logs and must not publish/deploy production without explicit approval.
