# Provider Profiles

Profiles are split by scope:

- `generic/` contains reusable provider/profile templates.
- `providers/` contains model or AI-client provider profiles.
- `apps/` contains external app/tool profiles.
