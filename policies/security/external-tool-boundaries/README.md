# External Tool Boundaries

External boundary policies are split by scope:

- `generic/` contains broad external-action boundary rules.
- `providers/` contains AI/model provider boundaries.
- `apps/` contains third-party app/tool boundaries.
