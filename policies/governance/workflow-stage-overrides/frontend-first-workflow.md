# Frontend-first Workflow

Frontend-first is allowed when visual/user experience discovery is the priority.

Allowed modes:
- sandbox only;
- mock API mode;
- imported frontend mode;
- draft branch mode;
- static UI mode.

Production integration still requires approved API/backend status or an explicit backendless waiver.
