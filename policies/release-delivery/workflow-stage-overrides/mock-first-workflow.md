# Mock-first Workflow

Mock-first workflows allow frontend, QA, and product review before backend is finished.

Requirements:
- mock contract documented;
- mock data stored in approved test-data location;
- API status marked mock_or_pending;
- downstream packets must not claim production integration;
- backend/API baseline must be approved before production release.
