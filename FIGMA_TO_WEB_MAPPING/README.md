# Figma to Web Mapping

This folder converts approved Figma baselines into web implementation instructions. It is separate from native mapping because web requires responsive breakpoints, CSS/token mapping, browser interaction states, SEO metadata, hover/focus behavior, and route-level decisions.
