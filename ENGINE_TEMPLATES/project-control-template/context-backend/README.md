# Project Context Backend

This folder is generated into real projects as `project-control/context-backend/`.

It contains configuration, scripts, schemas, retrieval packs, sync logs, and optional automation templates for AppForge context backends.

Canonical truth still lives in project-control Markdown/GitHub. Context backends retrieve and organize context.
