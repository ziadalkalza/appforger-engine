# Env and Secrets Quickstart

The single user-facing private file is:

```text
local-only/.env.local
```

Create it from:

```text
local-only/.env.local.example
```

Keep real secrets out of `project-control/`, `docs/`, RAG indexes, graph stores, and reports.
