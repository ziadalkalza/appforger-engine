# Bitbucket Integration Policy

Bitbucket Cloud uses API tokens by default. Bitbucket Data Center uses bearer Personal Access Token by default. SSH clone is supported for local clone/push, while REST/API fetch is used for remote-only context.
