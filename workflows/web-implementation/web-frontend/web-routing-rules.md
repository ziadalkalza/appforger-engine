# Web Routing Rules

Every route must be registered before implementation.

Required route metadata:

- Route path
- Page/screen name
- Auth requirement
- Data dependencies
- Loading/error states
- SEO metadata if public
- Breadcrumb/navigation behavior if relevant

Update `REGISTRIES/WEB_ROUTE_REGISTRY.md` for every route.
