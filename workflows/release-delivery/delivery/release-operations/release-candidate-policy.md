# Release Candidate Policy

A release candidate is a version believed to be ready for release if final verification passes.

Example:

```text
1.0.0-rc.1
```

When a release candidate is created:
- feature work is frozen
- only blocker fixes should be accepted
- release evidence must be collected
- final QA and production checks begin
- release blockers are tracked explicitly
