# Release Operations

This layer manages release planning, release candidates, versioning, release notes, blockers, hotfixes, rollback planning, and post-release monitoring.

Appforger coordinates and documents release work. It does not automatically submit to stores, publish production web deployments, or create paid resources without an approved automation layer and explicit user approval.
