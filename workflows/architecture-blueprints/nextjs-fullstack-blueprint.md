# Next.js Fullstack Blueprint

Use when a single web repo owns frontend routes plus limited server/API functionality.

Include:
- app/router structure
- auth/session strategy
- server/client component split
- database/backend boundary
- deployment target
- environment variables
- test strategy
