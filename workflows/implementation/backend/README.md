# Backend Implementation Workflows

Backend workflows are split by scope:

- `generic/` contains backend rules that apply across stacks.
- `stacks/` contains framework or stack-specific workflows.
- `apps/` contains third-party backend platform workflows.
