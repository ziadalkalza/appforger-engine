# Context Backend Example

This compact example demonstrates Appforger v16 context backend behavior:

1. Index project-control Markdown.
2. Exclude secrets and local-only content.
3. Store source metadata with every chunk.
4. Retrieve a feature context pack.
5. Detect stale context conceptually.

The example uses local JSONL indexing by default. Qdrant setup is documented separately.
