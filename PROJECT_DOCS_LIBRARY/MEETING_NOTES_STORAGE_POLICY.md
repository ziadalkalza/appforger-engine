# Meeting Notes Storage Policy

Meeting notes should be Markdown by default because Markdown is easy to diff, review, and index.

DOCX/PDF meeting notes are allowed, but AppForge should create a Markdown companion summary for indexing.

Meeting notes live in `docs/meeting-notes/` or an approved external docs storage provider. `project-control` stores metadata only.
