# FastAPI + Postgres Team Operations Profile

Use this when you want custom API control, deployment flexibility, and explicit backend ownership.

Recommended for production teams that already operate FastAPI/Postgres.

This profile provides SQL starter schema and API contract templates. Route skeletons are not production-complete and should be generated through backend execution packets when selected.
