# Team Operations Backend Policy

The operations backend is optional. File/GitHub project-control mode remains valid.

Use the operations backend when live filtering, dashboards, approval queues, automation visibility, or sub-agent status tracking are needed.

The backend must not store full canonical documents or secret values. It may store metadata and links to canonical artifacts.
