# Qdrant Collection Schema

Default collection:

```text
appforge_<project_slug>_memory
```

Recommended vector distance: cosine.

Payload metadata is required for filtering by current/approved source.
