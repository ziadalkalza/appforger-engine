# Raw Code Indexing Approval Policy

Raw source code indexing is disabled by default.

To enable it, AppForge must record:
- user approval
- repositories included
- excluded paths
- indexing purpose
- provider/storage target
- privacy/security review
- reindex/update workflow

If approval is missing, code agents should generate code summaries instead of indexing source files directly.
