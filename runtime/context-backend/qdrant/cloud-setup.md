# Qdrant Cloud Setup

Required references:
- `QDRANT_URL`
- `QDRANT_API_KEY`

Appforger uses Qdrant for derived context only: document chunks, code summaries, Confluence summaries, and retrieval metadata. It is not canonical truth.

Suggested collections:
- `appforge_docs`
- `appforge_code`
- `appforge_confluence`
- `appforge_mixed_context`
