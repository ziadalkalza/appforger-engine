# Neo4j Aura / Cloud Neo4j Setup

Required references:
- `NEO4J_URI`
- `NEO4J_USERNAME`
- `NEO4J_PASSWORD`

Appforger uses Neo4j for relationships and impact analysis:
- Requirement -> Screen -> Component -> API client -> Backend endpoint -> Service -> Model -> Test.
- ConfluencePage -> Requirement/UserStory/Feature.
- CodeSource -> CodeFile -> Symbol.

Graph data is derived from docs/code/context, not a replacement for canonical docs or repos.
