# Web Accessibility Rules

Web implementation must support accessibility unless the task is explicitly prototype-only.

## Required checks

- Semantic HTML
- Keyboard navigation
- Focus visible states
- ARIA only when needed
- Color contrast
- Form labels and errors
- Screen reader-friendly status updates for important state changes
