# Context Reindex Job Template

Purpose: reindex approved project-control memory into configured context backend.

Must exclude secrets, local-only debug files, build artifacts, and drafts unless explicitly enabled.
