# AI Prompt Templates

Prompt templates are split by scope:

- `generic/` contains provider-agnostic prompts.
- `providers/` contains model/client-specific prompts.
- `apps/` contains third-party app or tool-specific prompts.
