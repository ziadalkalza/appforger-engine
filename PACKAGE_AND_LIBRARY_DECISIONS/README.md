# Package and Library Decisions

This layer controls when AppForge recommends external open-source/commercial packages versus building manually with platform/native code.

AppForge must ask the user and research current package options before adding a new dependency, unless the package is already approved for the project.
