# Team Change Request Policy

When a team member wants to change an approved artifact, open a change request instead of editing silently.

Change requests must describe:
- requested change
- reason
- affected artifacts
- affected roles
- required re-approvals
- rollback plan if needed
