# Source Pipeline Orchestrator

The source pipeline chooses the right workflow for a source. Confluence goes through doc fetch and indexing. Git/code sources go through code scan, map generation, RAG indexing, and graph sync.

Use `project-control/pipelines/scripts/run_source_pipeline.py` after setup.
