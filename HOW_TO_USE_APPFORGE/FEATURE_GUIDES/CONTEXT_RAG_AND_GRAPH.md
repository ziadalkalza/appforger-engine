# Context, RAG, and Graph

RAG stores derived searchable context. Graph storage stores relationships and impact maps. Neither replaces canonical docs or code.

Use RAG to retrieve relevant summaries. Use graph to answer dependency questions such as which screen calls which API or which requirement is tested by which test case.
