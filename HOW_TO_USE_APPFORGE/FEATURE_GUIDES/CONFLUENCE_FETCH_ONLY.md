# Confluence Fetch-Only Integration

AppForger can fetch Confluence Cloud or Data Center pages by space, page ID, URL, parent tree, or page-with-children.

Storage modes include `docs_mirror`, `exports_snapshot`, `local_only_mirror`, `rag_only`, and `metadata_only`.

The integration is fetch-only. It must not create, update, delete, or modify Confluence content.
