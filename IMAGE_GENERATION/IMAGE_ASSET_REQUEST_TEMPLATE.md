# Image Asset Request Template

```yaml
asset_id:
project:
stage:
asset_type: logo | icon | mascot | onboarding | empty_state | store_visual | other
purpose:
target_platforms:
style_direction:
brand_colors:
must_include:
must_avoid:
references:
number_of_variations:
transparent_background_required:
figma_import_target:
review_owner:
status: requested
```

## Prompt

```text
Create [asset type] for [app/project]. It should communicate [keywords]. Use [style direction]. Avoid [must avoid]. Generate variations suitable for review before Figma import.
```
