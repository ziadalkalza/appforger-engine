# Automation Example

This compact example shows a safe automation chain:

1. Run project-control validation.
2. Reindex context backend if enabled.
3. Generate a draft next-step packet.
4. Record job run evidence.

No job approves baselines, commits, pushes, deploys, or publishes.
