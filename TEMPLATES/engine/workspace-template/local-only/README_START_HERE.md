# Local-only private files

Copy `.env.local.example` to `.env.local`.

`local-only/.env.local` is the single user-facing local/private configuration file for AppForger runtime endpoints, access references, and local credentials.

Do not commit `local-only/`.
