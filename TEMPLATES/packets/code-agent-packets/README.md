# Code Agent Packets

This folder contains provider-agnostic code-agent packet templates and provider-specific packet adapters.

- Generic packet templates live directly in this folder.
- Provider-specific packet templates live under `providers/<provider>/`.
