# Provider Limitation Policy

Every provider profile must include limitations.

Examples:

- Chat-only providers cannot be treated as having applied/tested code unless connected to tools that actually do so.
- Code agents cannot approve baselines or release blockers unless explicitly authorized.
- Design tools do not replace source-of-truth registration in project-control.
- RAG/context tools retrieve candidates; they do not become canonical truth.
