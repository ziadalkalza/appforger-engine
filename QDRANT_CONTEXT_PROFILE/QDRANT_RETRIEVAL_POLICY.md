# Qdrant Retrieval Policy

Retrieval must filter for current and approved sources when the task needs canonical context.

Draft retrieval is allowed only when the user explicitly asks for draft exploration.

Every result must cite the source path and metadata.
