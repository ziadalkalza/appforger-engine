# Claude Code Packet Template

Use only when Claude Code is selected/available as the code-agent provider.

This packet must still follow Appforger source-of-truth, security, task completion, and evidence rules.

- Task:
- Repo:
- Context files:
- Files to inspect:
- Commands allowed:
- Commands forbidden:
- Tests to run:
- Done report required:
- Registry updates allowed:
- Stop conditions:
