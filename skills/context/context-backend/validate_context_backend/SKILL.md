# Skill: Validate Context Backend

## Purpose
Confirm context backend can be trusted for retrieval.

## Checks
- Config exists
- Secrets are not in Markdown
- Backend reachable if enabled
- Collection/tables exist
- Retrieval returns source metadata
- Freshness validation passes
- Stale results are marked
