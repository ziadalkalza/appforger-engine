# Context Skills

Context skills are currently generic, so they live directly under this folder:

- `code-context/`
- `context-backend/`

If app-specific context skills are added later, create an app-specific child folder at that time.
