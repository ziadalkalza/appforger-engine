# Backend Skills

Backend skills are split by scope:

- `generic/` contains backend skills that apply across stacks.
- `stacks/` contains framework or stack-specific skills.
- `apps/` contains third-party backend platform skills.
