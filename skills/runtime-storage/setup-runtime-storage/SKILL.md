# Setup Runtime Storage Skill

Use when configuring Postgres, Qdrant, or Neo4j. Read runtime-selection.yaml, inspect local-only/.env.local, then run appforge_runtime.py only after user confirms local/cloud mode.
