# Apply Adoption Plan Skill

Use this only after an adoption plan exists and the user has approved it. Physical move/copy/symlink adoption requires both recorded approval and CLI flags. Preserve `.git` directories and block on target conflicts.
