# Provider Skills

Provider skills are split by scope:

- `generic/` contains provider-agnostic routing, packet, and decision skills.
- `providers/` contains adapters for specific AI/model providers.
