# Visual Test Authoring Rules

Visual tests compare implementation output to approved Figma/design baselines.

Each visual test should record:

- design baseline version
- screen/frame name
- device/browser size
- implementation screenshot path
- differences found
- severity
- fix/retest status

Visual tests are required for UI tasks unless the task is explicitly non-visual.
