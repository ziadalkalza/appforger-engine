# iOS Test Authoring Rules

For SwiftUI/iOS features, consider:

- ViewModel unit tests
- API client tests
- validation/state-transition tests
- basic build check
- simulator/device manual checks
- accessibility labels and dynamic type where relevant
- screenshot evidence for visual QA

UI-heavy work can be verified through build + visual QA + manual checks when full UI automation is not cost-effective.
