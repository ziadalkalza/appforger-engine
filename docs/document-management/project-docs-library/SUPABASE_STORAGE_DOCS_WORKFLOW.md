# Supabase Storage Docs Workflow

Supabase Storage may be used for shared project documents.

Store document metadata in project-control registries and keep actual files in Supabase Storage buckets. Index Markdown summaries/extractions where allowed.

Never store secrets in document metadata.
