# Security and Secrets

Use `local-only/.env.local` for private local values. Project-control stores metadata and references only. Raw code indexing, sensitive Confluence indexing, auth/security/payment/migration/API-contract changes, cleanup, volume deletion, Git push, and cloud provisioning are high-risk actions that require approval.
