# Provider Adapters and Agent Bootstrap

Before setup, ask your AI model to read `docs/getting-started/start-here/START_HERE_FOR_AGENTS.md`. After setup, AppForger generates provider-specific adapters such as `AGENTS.md` for Codex or `CLAUDE.md` for Claude Code.

Generate only the adapter for the tool you are using.
