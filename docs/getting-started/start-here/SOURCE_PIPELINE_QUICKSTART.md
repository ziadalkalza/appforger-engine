# Source Pipeline Quickstart

Run registered sources through one command:

```bash
python project-control/pipelines/scripts/run_source_pipeline.py --source product-space
python project-control/pipelines/scripts/run_source_pipeline.py --source backend-main
```

The pipeline reads source registries and routes docs/code into the selected storage units.
