# Git Providers and Bitbucket

Appforger has a Git provider abstraction for GitHub, GitLab, and Bitbucket. Bitbucket support includes Cloud/Data Center configuration, URL normalization, API token metadata, remote file fetch scaffolds, clone fallback, and pipeline artifact templates.
