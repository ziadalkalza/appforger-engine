# Documentation Policy

Documentation is required when a feature is user-facing, admin-facing, API-facing, release-impacting, security/privacy-sensitive, or setup/deployment-relevant.

Documentation should be updated in external `docs/` or the app repo if implementation-specific.
