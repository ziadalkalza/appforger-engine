# Context Retrieval Explanation Policy

Retrieval results should explain why each item was returned.

Examples:
- semantic match to query
- matching feature_id
- matching artifact_type
- current approved baseline
- linked user story/API/screen
- recent related done report

A lightweight explanation is enough, but the user or agent should understand why the item is relevant.
