# Context Backend Security

Do not index:

- `.env` files
- secrets, tokens, API keys, private keys
- build artifacts
- node_modules
- local debug logs
- unapproved drafts unless explicitly enabled

Cloud context backends require security review and indexing-scope approval.
