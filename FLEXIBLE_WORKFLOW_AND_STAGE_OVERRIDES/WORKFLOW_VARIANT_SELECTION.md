# Workflow Variant Selection

Workflow variant may be selected at project start or changed later.

Changing variant requires:
- decision record;
- impact report;
- stage override registry update;
- affected packet review;
- baseline freshness check;
- user approval.

The engine should ask when uncertain rather than force the default path.
